pipeline {
			agent { 
    label 'Slave1'
  }
  
			stages {								
				stage('Build') {
    					steps {
                    echo "Test Stage"
                    }
    					    
    					}
				}
			}
