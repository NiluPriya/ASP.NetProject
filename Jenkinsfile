pipeline {
			agent { 
    label 'master'
  }
  
			stages {								
				stage('Build') {
    					steps {
                    echo "Test Stage"
                    }
    					    
    					}
				}
			}
